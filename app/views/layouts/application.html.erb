<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Bear Books" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Bear Books" %>">
    <%= stylesheet_link_tag "application" %>
    <%# Foundation 5 won't work with Rails Turbolinks %>
    <%# Modernizr is required for Zurb Foundation %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>
  <body>



    <header>
      <%= render 'layouts/navigation' %>
    </header>

    <main role="main">

       <%= render 'layouts/messages' %>
      <div id="wrap">
        <div id="main">
       <%= yield %>
        </div>
      </div>
    </main>

    <div id="footer">
               <hr/>
      <div class="row">
        <div class="small-6 small-centered columns"> <h3> Thank you for visiting Bears Books. Developed By <em>Vigoo</em> @ <a href="wustl.edu"> WUSTL </a> :) </h3> </div>
      </div>
    </div>

    <%# Foundation 5 won't work when application.js is included in HEAD %>
    <%= javascript_include_tag "application" %>
  </body>
</html>
